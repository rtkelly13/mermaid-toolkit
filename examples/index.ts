/**
 * Runs every example in sequence. This file is what `pnpm examples` executes,
 * and it doubles as a smoke test that the public API stays runnable.
 *
 * Run with: `pnpm examples`
 */
import './ascii-rendering';
import './theming';
